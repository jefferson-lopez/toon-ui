{
  "name": "@toon-ui/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "toon-ui": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@toon-ui/core": "workspace:*"
  }
}
